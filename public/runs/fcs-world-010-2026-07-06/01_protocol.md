# Protocol — fcs-world-010-2026-07-06

Status: registered, sealed, live and open; two first-party attempts anchored.

## Purpose
The first probe on this record that is UNSOLVABLE by any function of the observed variables. Four observables (a,b,c,d) are driven by a HIDDEN common cause plus one real observed edge. Observationally everything is correlated; only interventions reveal that no observed variable causes the others. The correct frame requires positing an object not in the data — the literal definition of frame construction, tested directly for the first time.

## Grading
Mechanical: exact prediction of held-out interventional outcomes (all four observables per probe). A solver frame-locked on observed causation mispredicts; one that posits the latent common cause and finds the b->d edge predicts exactly. Whether the attempt posited a latent is recorded but never scored — only predictive consequences are.

## Honest scope
Positing a latent confounder when interventions expose observed insufficiency is a STANDARD causal-inference move (do-calculus), present in training corpora. This world therefore tests ontology EXPANSION within a handed meta-frame (causal graphs with latents) — deeper than the function-identification of worlds 001-009, but NOT the invention of a novel meta-frame. See the run bundle and Dispatch No. 019 for the deeper bound this probe surfaced about mechanical gradeability itself.
