# Operating Surface Repair - 2026-07-03

The operator reviewed the first operational-surface implementation and rejected
most of its expression while preserving part of its structure.

## Keep

- Typed ontology in `src/data/ontology.ts` and `/record.json`.
- MCP `get_ontology` and machine interop updates.
- Public `/ontology`, `/challenges`, and `/handoff` routes as durable addresses.
- Run lifecycle, evidence provenance, forecast horizon, probe family, and
  machine-channel instruments as reusable inspection aids.

## Rewrite

- "Operating system" becomes record atlas.
- "Challenge court" becomes challenge register.
- "Builder cockpit" becomes handoff note.
- "Command surfaces" becomes record trail or inspection instruments.
- Heavy grid-panel styling becomes quieter Observatory-native framing.

## Remove

- The home-page ontology graph. It interrupted the primary reading of the
  operating question and made the first viewport feel like a product demo.
- Stale World-003 assumptions after World-007 became the current front of the
  run record.

## Missing

- More visual beauty should come from the sealed-world and Proving Ground
  imagery already in the record, not from generic dashboard chrome.
- Future additions should begin from the Observatory's own metaphors: dome,
  ledger, seal, aperture, run, correction, and trail.

No verdict movement is implied by this repair.
